# PathogenIQ environment configuration
# Copy to .env and fill in your credentials.
# .env is in .gitignore — never commit credentials.

# ── Alert: Slack ─────────────────────────────────────────────────────────────
# Create an incoming webhook at https://api.slack.com/messaging/webhooks
PATHOGENIQ_SLACK_WEBHOOK=

# ── Alert: Email (SMTP) ───────────────────────────────────────────────────────
# For Gmail: use an App Password (https://myaccount.google.com/apppasswords)
PATHOGENIQ_SMTP_USER=
PATHOGENIQ_SMTP_PASS=
# Comma-separated list of alert recipient addresses
PATHOGENIQ_ALERT_EMAILS=

# ── Dashboard Auth ───────────────────────────────────────────────────────────
# HTTP Basic Auth for the web dashboard (browser will prompt for credentials)
# Set PATHOGENIQ_DASH_AUTH=false to disable auth entirely (e.g. behind a VPN)
PATHOGENIQ_DASH_USER=admin
PATHOGENIQ_DASH_PASS=pathogeniq
PATHOGENIQ_DASH_AUTH=true

# ── Watcher ───────────────────────────────────────────────────────────────────
# How often (seconds) to poll the data directory for new reports
WATCH_INTERVAL=3600
